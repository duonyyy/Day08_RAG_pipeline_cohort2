[project]
name = "day08-rag-pipeline-cohort2"
version = "0.1.0"
description = "Day 8 RAG pipeline exercises for legal and news retrieval."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[tool.uv]
package = false
